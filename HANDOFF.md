# PSRTTY 0.84 正本・引き継ぎ

唯一の基準：PSRTTY_0.84_CANONICAL_SOURCE.zip。
0.84実装内容を統合済み。旧版や差分／FIXの再適用による再構築は不要です。
次の開発版は0.85（別の指定があればそれを優先）。要望を蓄積し、実装指示後に着手してください。
同一の配布ファイル名で仕様の異なるFIXを差し替えないでください。

## 固定事項
- 日本語Windowsアプリ。Python 3.12 / PySide6 / PyInstaller。通常更新は変更ファイルだけの差分ZIP。
- クロススコープ：別ウィンドウ、ボタン開閉連動、起動時OFF。主スペクトラム幅を維持。
- MARK/SPACE各方向で最新＋1つ前の2本、最大4本。前の輪は45%を基準に減光。取得時刻から最大0.7秒で消去。同じフレームの再読みによる寿命延長なし。
- 同調時の細い楕円、離調時の変化を維持。解析5 Hz／フェード20 Hz。位置変更・入力停止・非表示で残光を消去。
- 画面・手動入力・最新QSO・QSOログ・生ログはJST固定。ADIFはUTC日時と交信のUTC年月で保存。
- メニューは今月のADIFを開く（UTC基準）／今日の生ログTXTを開く（JST基準）。クリック時に対象を決定。
- 0.77以前の既存ADIFは自動再分配しない。月を変えるQSO編集は未知フィールドを維持し、両ファイルをバックアップ。詳細TIME_POLICY_078.md。
- SQ初期4。TU73自動記録は未接続テストでも可能。CALL空欄では記録しない。
- 送信中STOP・PTT解除・終了待機の修正を維持。設定画面を開くと切断する現行仕様は変更なし。
- UIテストは予期しないダイアログを検出。UTF-8を明示し、終了バックアップの完了前に一時フォルダーを消さない。
- ICOM機種対応はRADIO_SUPPORT_077.md。YAESUはDNR/DNF/NOTCH表示、NB共通。
- Cabrillo仕様はCABRILLO_SPEC.md参照。


## 0.84変更
- マクロ編集の保存ボタン上にSENT欄と固定チェックを追加。
- テンプレート反映時：通常は空欄、JARL WWは01、CQ WWは25。すべて固定ON。編集可能。
- 選択のみでは変更しない。保存でメイン画面へ反映し、キャンセルで破棄。既存設定は開くだけでは変更しない。
- マクロ編集、バックアップ設定、基本・無線機・Audio設定のSave/Cancelを保存/キャンセルへ変更。
- Windowsでのビルド・実機確認は利用者側で実施。
