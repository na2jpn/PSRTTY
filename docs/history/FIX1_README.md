# PSRTTY 0.81 FIX1

0.81のソースへZIP内容を上書きし、テスト後build-windows.ps1で再ビルドしてください。0.79へ直接適用せず、先に0.81差分を適用してください。設定・ログは変更しません。バージョン表記は0.81です。

クロススコープを縦横の楕円型に修正しました。MARKは横長、SPACEは縦長。同調のずれに応じて傾きます。単一音では片方だけ、無音では非表示。雑音・遷移中の輪郭は乱れます。

以前の非重複・位相ゼロ帯域フィルターを、帯域が重なる複素共振応答と位相補償に変更。各トーンが両軸に届き、設定周波数で90度の位相差となるよう調整します。装飾の楕円描画ではありません。点表示から連続軌跡に変更。MMTTYのアルゴリズムを複製したものではありません。

受信デコード、SQ、送信、日時、ログ処理は変更なし。初期設定ガイドを更新。

参考（表示の意味）：https://dxlabsuite.com/winwarbler/Help/SoundcardRTTYReception.htm

検証：単一MARK/SPACEの楕円、無音、40 Hz離調時の傾き、周波数の上下逆転、デコード入力不変、開閉連動を自動テスト。合成信号のQt描画も確認。Windows実機・実際の受信音での最終確認は未実施。

全236テスト：235成功・既存1スキップ（26.095秒）。継承による既存UIテスト再実行を含みます。
