# Ver0.79

- スペクトラムの上から2本目（上端から40%）の横罫線を、見分けやすい黄土色に変更。太さは従来の1px。
- 初期設定ガイドに波形の高さ・表示感度の調整目安を追加。
- デコード・SQ・音声入力・ログの処理は変更なし。

# Ver0.78

- JST固定表示・入力、ADIFのUTC月別保存を統一。
- 月別メニューの基準明記、ファイル名表示撤去。
- QSO日時編集でUTC月が変わる場合の移動とバックアップ。
- YAESUのDNR/DNF/NOTCH表示。

# Ver0.77

- Cabrillo日時ラベルと入力欄の大きな隙間を解消。
- IC-7200 / IC-7410 / IC-7600 / IC-9100追加。
- IC-7200のDATA切替は専用コマンドを使用。追加４機種のフィルターは本体操作。
- 0.76 FIX1（UTF-8ログ読込とテスト終了待機）を同梱。

# Ver0.76

0.75 FIX1を基にCabrilloの日時・エラー表示・大会期間案内を改善。
汎用開始は表示ゾーン(JST)で月初を計算。画面は分精度、終了境界は59.999999秒。
QSO日時が表示値から未編集なら元の秒を保持。編集時は指定分の00秒。
診断ログはvar/cabrillo_YYYYMMDD.logへ。書込失敗は操作を妨げない。
入力検証は日時/周波数を分離し、セル背景とツールチップ・状態欄に案内。
年はボタン表示に連動。注意書きは専用形式のみ。日程計算ルール自体は変更なし。
0.75 FIX1の非対話テスト境界とガイド7タブの修正を継承。

---

# Ver0.75

正本PSRTTY_0.75、ソース差分は0.74へ適用。
バックアップ初期件数30（既存値維持）。Cabrillo出力4画面を追加。
psrtty/cabrillo.py: 形式別検証・文字列生成・原子的保存。
psrtty/ui/cabrillo_dialog.py: QSO抽出/選択/コピー編集、提出情報、プレビュー/保存。
提出情報は正常保存後にconfigのcabrillo.<profile>へ保存。
日時の抽出はUTCで比較、表示のみJST/UTC切替。期間端は含む。
TX番号やX-QSOは出力時の属性で、ADIFへ追加しない。
JARL大会識別名はDXLog開発元の対応表を参照、交換番号はJARL規約から定義。
公式仕様と残る確認範囲はCABRILLO_SPEC.mdに記載。
追加テスト11、全203（成功202、環境制約skip1）。Windows実機ビルドは未実施。
ICOM機種の追加は今回の対象外。

---

# Ver0.74

正本PSRTTY_0.74、差分は0.73へ適用。

- マクロのテンプレート選択欄の上に「選択→反映→Save」の案内。
- ADIFをPC現地日付の保存月ごとYYYYMM.adiへ追記。生ログはYYYYMMDD_all.txtの日別を維持。
- ファイルメニューに今月のADIF/今日の生ログTXTとファイル名。開くたびに日付更新。
- 編集→マクロ編集の下にバックアップ設定。終了時ON、件数ごとOFF/100件を初期値とする。
- 新規保存QSOの件数を設定へ保存、再起動で引継ぎ。成功したバックアップでリセット、失敗時保持。
- 手動/指定件数/終了時にADIF・生ログをZIP保存。logdata/backupsへ一意名、検証後に原子的確定。既存バックアップを含めない。
- 起動時に実ファイル作成・書込・flush/fsync・削除。失敗ならダイアログ表示、メイン画面なしで終了。

backup.py: check_log_writable/create_log_backup。
backup設定: on_exit/every_enabled/every_count/pending_qsos。
GUIスレッドでスナップショット取得。アプリのログ書込もGUIで直列化され、取得中の内部書込はない。
BackupDialogのSave失敗はダイアログを閉じず入力保持、メモリ設定を戻す。
終了時設定保存が失敗しても後続の停止・バックアップを実施する。
ADIF旧日別ファイルもglobで読み取れるが、変換・削除はしない。

---
過去の履歴:

# Ver0.73

正本: PSRTTY_0.73。差分は0.72へ適用。

- CQ WW RTTY DXコンテストのF1～F9テンプレート追加。JARL用と同じ操作順で、SENTにCQゾーンを設定。
- マクロ編集のF9下に青字のテンプレート別案内。JARLは年齢/01、マルチオペ平均年齢、00/99も可を説明。CQ WWは日本国内25（小笠原・南鳥島を除く）、SENT固定、599不要を説明。
- 選択だけでは案内だけ変更。反映→Saveでマクロと適用テンプレートを保存。Cancelでは既存設定・マクロを保持。
- CQ WW用適用中は、RSTに続くゾーンと公式の州・地域略号をRCVDへ取り込み（05 MA、04 ONなど）、ADIFのSRX_STRINGに保持。
- 初期設定ガイドを更新。SQ初期4、未接続TU73自動ログ、STOP処理は維持。

macro_templateは反映したテンプレート名をSave時にconfigへ保存。表示中の選択と適用済みを区別。
既存キーなしは通常交信扱い、既存マクロの移行上書きなし。
parser.parse_exchange(cqww=True)はゾーン1～40と隣接QTH（ゾーン反復可）のみ連結。
QTHは米国本土48州/DCとカナダ14地域。一般文やコールは連結しない。
MainWindowの自動取り込み/カード選択の両方に適用。通常/JARLは従来の1項目。
マクロダイアログは820×590へ拡大、折り返す青字案内を表の下に配置。

資料（2026-09-24確認）:
- https://www.jarl.org/Japanese/1_Tanoshimo/1-1_Contest/rtty_rule.html 第5項
- https://cqwwrtty.com/rules.htm III、IV.C.3
- https://cqww.com/cq_waz_list.htm ゾーン25/27

---
過去の履歴:

# Ver0.72

- 自動ログ追加から無線機の接続条件を撤廃。未接続でも自動ログON・相手CALLあり・TU 73／TU73の正常送信完了で追加。
- 未接続で周波数が不明の場合は、ADIFのFREQ・BANDを省略して記録。推測値を入れない。
- STOP・送信失敗・相手CALL空欄・自動ログOFFでは追加しない。送信中の手動編集/周波数変更による取消しと、送信tokenによる重複防止を維持。
- SQ初期値を4に変更。保存済み設定とSQ判定特性を保持。
- 初期設定ガイドの未接続テスト説明とSQ初期値を更新。

# Ver0.71

- 自分の送信文のTU 73／TU73を検出し、正常送信完了後に自動ログ追加。マクロ・手動送信に対応し、旧QSO完了フラグに依存しない。
- 自動ログON・相手CALLあり・無線機接続中が条件。STOP・失敗・送信中の手動編集/周波数変更では追加しない。未接続テストは従来どおり対象外。
- 日付と時刻を常時表示。「日時手動」で更新停止・編集、OFFで現在日時へ戻る。自動中はコロンが2秒点灯・2秒消灯。保存成功後は自動へ戻り、保存失敗時は入力を保持。
- SQ初期値を3に変更。0.70の判定特性と、保存済み設定は保持。
- 初期設定ガイドを更新。マクロ編集の旧QSO完了列は非表示にし、送信本文で判定する説明へ変更。

# Ver0.70

- デコードSQの低段階を緩和。OFF・1～10、初期2と保存済み設定を維持。
- 送信中の受信文字・自動取り込みを抑止し、ノイズで交信入力が変化して自動ログを取り消す経路を修正。送信終了時はデコーダをリセットし、送信中の古い受信通知を無効化。
- 手動編集・周波数変更・CALL空欄・QSO完了指定なし等、自動ログ未追加の理由をステータスと通信記録へ表示。STOP・送信失敗・未接続テストで追加しない条件を維持。
- 最新QSOの左右にNo.列。日別ADIFのファイル名順＋ファイル内記録順で先頭1から採番。大きい番号から左上→右上→次行に表示。
- 最新QSO枠の右下の罫線上に「ログ合計 XX件」。200件の読み込み上限を撤廃し、全日全件を集計。削除後は番号を詰め直す。
- 初期設定ガイドを更新。表示感度は波形の表示だけでSQ感度とは無関係であることを明記。

# Ver0.60

- STOP時の音声終了処理を変更。GUIからsounddeviceのグローバルstopを呼ばず、停止要求だけを通知。送信スレッドが専用OutputStreamの再生・中止・PTT解除・終了を順番に行う。
- マクロ、手動送信、Auto CQ、両STOP、Escで同じ停止処理を使用。中断時の自動ログ抑止を維持。
- Centerの短い線とその周囲をドラッグ可能に。MARK／SPACEの間隔を保って一緒に移動。手のカーソルを表示。
- デコード枠線上に「SQ［2 ▼］ ☑ デコード」。OFF・1～10、初期2、設定保存。SQ専用メニューは追加しない。
- デコーダは開始ビットとストップビットを確認してから文字を確定。トーンの集中度によるSQでノイズ文字を抑制。
- 初期設定ガイドへSQ調整、Centerドラッグ、STOPの説明を追加。

# Ver0.50

- 0.40のtest_latest_counts_gap_and_log_linkで発生したQMenu already deletedを修正。親が所有するQMenuをfindChildrenで直接取得し、一時QAction.menu()参照を経由しない。
- デコードチェックをグループボックス上辺の右側へ移動。チェック背面で罫線を覆い、内側の専用行を廃止。
- スペクトラムのMARK／SPACE中央に、上端から12pxの控えめなグレー線を追加。
- 波形下にMark / Center / Spaceを表示。Centerは両トーンの平均値で、移動・幅変更・AUTO TUNE・RESETに連動。0.5Hzも表示可能。
- 初期設定ガイドへ中央表示とチェック位置の説明を反映。

# Ver0.40

- デコード本文を1段階縮小（初期12pt）。時刻・RX/TX表示サイズは維持。既存の文字サイズは更新時に一度だけ2pt縮小、下限10pt。
- 接続表示を切替ボタン化。未接続は薄い青灰色＋オレンジ字、接続は水色＋青字。接続中は連打不可。
- 接続機種・COMポートはツールチップ。通信断の日時・理由も保持。
- 最新QSO件数は2／4／6／8／10、初期4件。旧3／5件は4件へ移行。左右合計の件数。
- 最新QSOを左右独立した表にして14pxの間隔。初期4件は2行×2列。狭い画面は1列。
- 最新QSOをダブルクリックでQSOログへ移動。「QSOログ一覧」を「QSOログ」へ統一。
- ヘルプの単独チューニング項目を削除。初期設定ガイドの「受信・チューニング」に集約。

# Ver0.30


- コントロールにFILTERを追加。ICOMはFIL1／FIL2／FIL3、YaesuはWIDTHとNARROW。
  機種・モードに応じた候補を表示し、実際の状態を読み戻します。状態未取得／非対応は操作不可。
  ICOMはモードとDATA設定を保持。送信中・Auto CQ中は操作不可。
  FT-991/A・FTX-1は引き続き試験用・実機未確認です。
- 最新QSOは横幅が足りれば2列、新しい順に左→右→次の行。狭い画面は1列。
  表示件数は左右合計です。従来の3／5／10件に6件を追加し、3行×2列も選べます。
- ヘルプの「FT8環境からの設定方法」を削除し、初期設定ガイドへ今回の操作を追記。
- デコード枠右上にチェック（起動時ON）。OFFで受信文字・RXカード追加・自動取得を停止。
  音声入力・スペクトラム・RXメーター・送信・既存カードは維持。
  再開時は停止前の処理待ち文字とデコーダ状態を引き継がず、その時点から再開。
- 無線機の周波数応答は3回連続取得失敗で切断。途中で応答が戻れば失敗回数をリセット。
  コントロールを開いている場合も同じ判定。切断日時・理由はステータス、接続表示の
  ツールチップ、およびlogdata/YYYYMMDD_all.txtへ残します。時間経過だけでは切断しません。
- 日付（YYYY-MM-DD）と時刻（HH:MM）を分離。両方空欄なら現在日時、片方だけなら入力案内。
  QSO完了時の自動設定・保存成功後の両欄クリア・保存失敗時の保持は従来どおり。
  PC現地時刻で入力・表示し、ADIFはUTC保存。
- カードは「時刻 RX | 内容」のコンパクト表示。長文は本文の開始位置へ折り返し。
  RX文字は濃い緑、TX文字は赤、TX背景は濃い朱色。最大30件は従来どおり。

検証結果と実機での確認事項はVALIDATION.mdを参照してください。



# Ver0.21 (2026-09-24)

- 二重起動時の呼び戻し応答がすでに届いている場合／分割到着する場合を正しく処理。
- 呼び戻しテストを別プロセスで実行し、応答完了までGUIイベント処理を継続。
- テストの成功・失敗・スキップにかかわらず子プロセス・サーバー・ロックを解放。
- FILTER切り替えは次の構想として保留。

# Ver0.20 (2026-09-24)

- 同一フォルダーの二重起動案内・起動済み画面呼び戻し。
- 周波数の桁区切り、円周ドラッグのジョグ、手前＋のホイールと保存される反転、1 Hz STEP。
- NOTCHグループ、GHzバンドラベル。
- スペクトラムの説明・線ドラッグ・受信シフト幅・独立した幅／位置RESET。
- スペクトラム固定縦軸・表示感度・表示平均化。
- QSO一覧の日時とBAND併記、編集／削除、元ADIFのバックアップ・対象更新・外部変更検出。
- 初期設定ガイドを6タブと表へ整理。
- 0.10の利用者設定・マクロ・ログ・更新再起動処理を保持。

# Ver0.10（実験版）

- 接続テスト後の接続ボタンと案内を追加。
- 単一のコントロール画面：周波数・バンド・ジョグ・STEP・NB/NR/NOTCH。
- デコードへ改称、QSO日時入力と最新QSOの年月日表示。
- F4でTU 73、罫線、新テンプレート、QSO完了フラグと送信成功後のログ。
- 再起動後の画面起動確認・結果記録を追加。
- 既存マクロ・設定・ログを保持。新しいテンプレートの反映は利用者が選択。

# Ver0.05 — 2026-09-24

- 基本設定タブとMYQTH／MYJCCJCG／MYTXTを追加。自局コールはメインと共有。
- 通常交信テンプレートを新規初期値に採用。既存マクロは保持、JARLテンプレートと切替可能。
- Audio INの未設定と、無線機接続から独立した非同期入力開始・停止を追加。
- 未接続の音声テスト用チェックボックスを追加。マクロ／手動TXのみ許可しAuto CQは接続必須。
- Auto CQを通常入力欄へ変更。1～99の補正と不正入力時の初期値復帰。
- Helpにバージョンアップ説明画面を追加。成功後の再起動、同版・異内容の修復に対応。
- 初期設定ガイドを基本設定からの7項目へ整理。テンプレート直後にAuto CQ説明。
- 114件の自動テストを通過。0.04旧更新処理との模擬互換性を確認。
- 0.04→0.05の初回更新だけは旧処理のため手動再起動が必要。

# Ver0.04 — 2026-09-24

- メニュー余白削減、重複CALL欄削除と自動取得チェック移動、最新QSO表示の高さ確保。
- Auto CQ追加：F1送信、1～99回（初期10）、終了後の間隔7秒、STOP/他送信/切断/異常時停止。
- SENT固定（初期ON）、OFF時はログ確定成功後に数値を+1。入力変更をマクロ表示へ即時反映。
- IC-7300MK2 B6、IC-905 ACと6バイト周波数応答を追加。
- FT-991 / FT-991A、FTX-1シリーズのCAT対応を試験用として追加。機種固有設定をグループ化。
- COMデバイス名表示、速度表示を自動に統一。
- Windows Audioを有効なWASAPI機器に絞り、Endpoint IDで保存。旧番号指定は再選択を要求。
- Audio IN/OUTの説明に信号の方向と録音／再生デバイスの例を記載。
- PTT解除失敗は送信失敗として通知し、Auto CQを継続しない。
- 0.03互換の配布ZIP仕様を維持。ビルド出力はPSRTTY_0.04.zipのみ。
- 自動テストと0.03旧更新コードによる模擬更新を追加。Windows・無線機実機は未確認。

# PSRTTY Change Log

## Ver0.03 — 2026-09-24

- ビルドの完成物をZIPのみへ変更。一時展開フォルダーは自動削除、既存releaseフォルダーは保持。
- 配布JSONをvarへ移動。0.02からの手動移行・旧JSONのバックアップ移動、新形式のZIP更新に対応。
- 標準ウィンドウ1280×800、状態保存・復元、画面外補正、小画面スクロール対応。
- --reset-windowと非モーダルの起動オプションHelpを追加。
- RXレベルをAUTO TUNE左へ移動。
- マクロを指定の6行配置へ変更。F8既定KKK、旧未編集F8のみ移行、自由編集を維持。
- 0.02を基準とした差分ZIPで提供。Windows・実無線機・実音声の検証は未実施。

## Ver0.02 — 2026-09-24

- 0.01正本のソースを直接更新。採用アイコン・復調器・ログ形式を維持。
- 初回の無線機を未選択へ変更、機種標準CI-Vアドレスの入力と手修正を維持。
- 接続・接続テスト・周波数取得を非同期化、タイムアウトとキャンセルを追加。
- 未接続時の送信ボタン／Fキー／内部処理を禁止。PTT ACK失敗時は音声を出さない。
- 接続時LSB-D自動切替ON、設定の高度な設定でUSB-Dも選択可。
- Help3項目と設定を非モーダル化。
- F1～F9化、旧8マクロを保持、編集可能なJARL WW RTTYテンプレートを追加。
- 自動ログをF4固定からTUマクロの正常送信完了時へ変更。失敗・STOPでは保存しない。
- build-windows.ps1で配布ZIP生成、Python失敗時停止、旧releaseフォルダー保持。
- ZIPからバージョンアップを追加。製品／版／パス／ハッシュ検証、バックアップ、データ保持、復元。
- 設定移行フックと同一フォルダーの実行／更新排他を追加。
- 自動テストとWindows実機検証手順を追加。

## Ver0.01 — 2026-09-24

Initial implementation baseline.

- ICOM CI-V + USB Audio oriented architecture
- Amber/orange PSRTTY UI and adopted icon
- RTTY AFSK TX and initial 45.45 baud / 170 Hz decoder
- Spectrum, AUTO TUNE, 170 Hz RESET
- Card-based RX/TX presentation and automatic callsign/exchange pickup
- F1-F8 contest macros
- Pipe Separator format raw transcript (`YYYYMMDD_all.txt`)
- ADIF QSO persistence (`YYYYMMDD.adi`)
- Settings tabs: 無線機 / Audio / 高度な設定
- QSO recent list and log viewer
- Windows PyInstaller build script

PSLog PATH integration and contest-specific Cabrillo definitions are intentionally deferred.
